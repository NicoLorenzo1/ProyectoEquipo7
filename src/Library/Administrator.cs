namespace Library
{
    public class Administrator
    {
        private List<User> Classic = new List<User>();
        private List<User> Bomb = new List<User>();

        private List<User> TimeTrial = new List<User>();

        private List<User> Challenge = new List<User>();

        //private List<Game> CurrentGames = new List<User>();

        private void MatchPlayers()
        {   
            foreach (User user in Bomb)
            {
                if (Bomb.Count > 2)
                {
                   // Bomb[1], bomb[2] 
                }
            }
        }

    }
}
